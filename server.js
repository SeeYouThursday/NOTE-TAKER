const express = require("express");
const path = require("path");
const termData = require("./db/terms.json");

const app = express();
const PORT = 3001;
//routes
////get notes

////posts new note

app.listen(PORT, () => {});
